services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - uploads:/app/public/uploads
    depends_on:
      - mongo

#  mongo:
#    image: mongo:7
#    ports:
#      - "27017:27017"
#    volumes:
#      - mongo_data:/data/db

volumes:
#  mongo_data:
  uploads:
